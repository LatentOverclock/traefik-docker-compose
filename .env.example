# Copy to .env and set real values.

# Public hostname for Traefik dashboard router
TRAEFIK_DASHBOARD_HOST=traefik.example.com

# htpasswd-compatible hash entry for basic auth.
# IMPORTANT: escape `$` as `$$` in .env for Docker Compose interpolation.
# Example raw hash:    admin:$apr1$abc$def
# Example .env value: admin:$$apr1$$abc$$def
TRAEFIK_DASHBOARD_USERS=admin:$$apr1$$REPLACE$$REPLACE
